{
  "name": "parkruneventsummary",
  "version": "1.2.0",
  "description": "Generate a shareable infographic from parkrun results, and a printable one-page volunteer roster for Run Directors.",
  "main": "script.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -f pes.zip pes.xpi && zip -9 -r pes.zip manifest.json src -x '*.DS_Store' && cp pes.zip pes.xpi"
  },
  "author": "Rich Boakes <rich@boakes.org>",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.54.0",
    "eslint-config-portsoc": "^1.0.1"
  },
  "eslintConfig": {
    "extends": "portsoc",
    "root": true,
    "env": {
      "browser": true
    }
  }
}
